x(1):=1$
x(1) :=1$
x(1):= 1$
x(1) := 1$

for all x,y let f(x,y)=0;
for all x,y clear f(x,y);
for all x,y such that x<0 let f(x,y)=0;
for all x,y such that x<0 clear f(x,y);

for all n1,n2 match x1^n1*x2^n2=f(n1,n2);

a where {f(~x)=>0,y=>0} when x>0;
